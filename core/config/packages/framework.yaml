# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # API без сессий — core stateless, реплики взаимозаменяемы.
    session: false

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
