framework:
    cache:
        # Общий кеш для всех реплик core — в Redis
        app: cache.adapter.redis
        default_redis_provider: '%env(REDIS_URL)%'

# Тесты без Redis: кеш в памяти процесса
when@test:
    framework:
        cache:
            app: cache.adapter.array
